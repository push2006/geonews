feedparser>=6.0.11
requests>=2.32.0
python-dateutil>=2.9.0
python-dotenv>=1.0.1
schedule>=1.2.2
pymongo>=4.8.0
Flask>=3.0.3
gunicorn>=22.0.0

# Installed by default so a single `pip install -r requirements.txt` /
# Render build works whether or not you turn these on. They're "optional"
# in the sense that they sit behind an ENABLE_*=true flag in .env, not in
# the sense that they're a separate install step.
trafilatura>=1.8.0   # used only if ENABLE_FULL_TEXT=true
gnews>=0.3.6         # used only if ENABLE_GNEWS=true
